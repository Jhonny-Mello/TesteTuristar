package com.br.Turistar;

import static org.junit.Assert.assertThrows;
import static org.junit.jupiter.api.Assertions.*;

import java.util.Optional;

import org.junit.jupiter.api.Test;

import com.br.Turistar.repository.UsuariosRepository;
import com.br.Turistar.service.UsuariosService;

class UsuariosTestes {

	/* @Test
   void whenDecrementIsCalledWithInvalidIdThenThrowException() {
       int quantityToDecrement = 10;

      when(UsuariosRepository.findById(System.)).thenReturn(Optional.empty());

     assertThrows(usuariosNotFoundException.class, () -> UsuariosService.decrement(INVALID_BEER_ID, quantityToDecrement));
   }*/


}
